<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.vodafone</groupId>
    <artifactId>EcomProxyApp</artifactId>
    <version>1.0</version>
    <name>Ecom Proxy Application </name>
    <url>http://maven.apache.org</url>

    <packaging>pom</packaging>

    <properties>
        <er.api.version>1.0-SNAPSHOT</er.api.version>
        <er.translog.version>1.0-SNAPSHOT</er.translog.version>

        <java.version>1.7</java.version>
        <slf4j.version>1.7.12</slf4j.version>
        <junit.version>4.12</junit.version>
        <spring.version>4.2.4.RELEASE</spring.version>

    </properties>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.vodafone</groupId>
            <artifactId>er-api</artifactId>
            <version>${er.api.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vodafone</groupId>
            <artifactId>er-trans-log</artifactId>
            <version>${er.translog.version}</version>
        </dependency>
        <!-- Add another for something like app-common -->
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vodafone</groupId>
                <artifactId>er-api</artifactId>
                <version>${er.api.version}</version>
            </dependency>
            <dependency>
                <groupId>com.vodafone</groupId>
                <artifactId>er-trans-log</artifactId>
                <version>${er.translog.version}</version>
            </dependency>
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>ecom-proxy-app</module>
    </modules>

    <build>
        <finalName>EcomProxyApp</finalName>
    </build>

</project>